import React from 'react';

const Filters = () => {

    const fetchGenres = async () => {
        
    }


    return (
        <div className={'filters'}>
            <div className={'filters__header'}>Фильтры</div>
        </div>
    );
};

export default Filters;